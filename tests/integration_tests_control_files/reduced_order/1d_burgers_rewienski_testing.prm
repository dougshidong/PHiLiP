# Listing of Parameters
# ---------------------

set test_type = POD_adaptation

# Number of dimensions
set dimension = 1

# The PDE we want to solve.
set pde_type  = burgers_viscous
set use_weak_form = true
set use_collocated_nodes = false

# use the grid refinement study class to generate the grid
subsection grid refinement study
 set num_refinements = 10
 set poly_degree = 0
 set grid_left = 0.0
 set grid_right = 1.0
end

subsection burgers
  set diffusion_coefficient = 0.012
end

#Reduced order parameters
subsection reduced order
  #set adaptation_tolerance = 0.0005
  #set adapt_coarse_basis_constant = 0
  set coarse_basis_dimension = 12
  set expanded_basis_dimension = 6
  set extrapolated_basis_dimension = 12
  set extrapolated_parameter_delta = 0.002
  #set fine_basis_dimension = 18
  set path_to_search = ./pod_basis/Jarvis_0.02/
  set method_of_snapshots = true
  #set consider_error_sign = true
end

subsection linear solver
  set linear_solver_type = direct
end

subsection flow_solver
  set flow_case_type = burgers_viscous_snapshot
  set final_time = 1.0
end

subsection ODE solver
  set initial_time_step = 0.02
  set nonlinear_max_iterations = 500
  set nonlinear_steady_residual_tolerance = 1e-12
  set print_iteration_modulo  = 1
  set ode_solver_type = implicit
end

subsection manufactured solution convergence study
  set use_manufactured_source_term = false
end


